[icingaweb_db]
type                = "db"
db                  = "mysql"
host                = "{{ icinga2.ido.dbserver | mandatory }}"
port                = "3306"
dbname              = "{{ icinga2.ido.dbname | mandatory }}"
username            = "{{ icinga2.ido.user | mandatory }}"
password            = "{{ icinga2.ido.pass | mandatory }}"
prefix              = "icingaweb_"

[icinga_ido]
type                = "db"
db                  = "mysql"
host                = "{{ icinga2.ido.dbserver | mandatory }}"
port                = "3306"
dbname              = "{{ icinga2.ido.dbname | mandatory }}"
username            = "{{ icinga2.ido.user | mandatory }}"
password            = "{{ icinga2.ido.pass | mandatory }}"
